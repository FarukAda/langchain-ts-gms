/**
 * MCP (Model Context Protocol) integration entry-point.
 *
 * @example
 * ```ts
 * import { createGmsMcpServer, startMcpServer } from "@farukada/langchain-ts-gms/mcp";
 * ```
 *
 * @module
 */
export {
  createGmsMcpServer,
  startMcpServer,
  type GmsMcpServerOptions,
} from "./server.js";
